# V7.2.3 Water Catalog Reads

The normalized JSON map catalog is retained as a SQLite comparison snapshot
after manual-water reconciliation. Map, Water Intel, and filtering continue to
return the JSON registry catalog by default.

`AI_WATER_CATALOG_READ_SOURCE` supports comparison and operator-gated staged
fallback testing. SQLite authority remains disabled.
